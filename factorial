# Recursion
??recursion basics
#include <stdio.h>
#include <stdlib.h>

int f=1;
int fact(int);
int main()
{
    int n,x;
    scanf("%d", &n);
    x=fact(n);
    printf("%d", x);
}
int fact(int a)
{
        if(a!=1){
        f=a*fact(a-1);
        return f;
        }
}
